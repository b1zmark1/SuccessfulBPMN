# Tasklist: точечные доработки по UI и worker

## 1) Проверка и выравнивание пайплайнов
- [x] Сравнить `workers/image_to_text_pipeline.py` с логикой `run_full_bpmn_pipeline.ps1` по шагам.
- [x] Зафиксировать, что Narrator для `image_to_text` работает всегда.
- [x] Поддержать режимы Narrator: `text` и `table` (UI -> `meta` -> worker).

## 2) Исправление `text_to_image` ошибки
- [ ] Диагностировать, почему `Text-to-image pipeline did not return PNG output`.
- [ ] Исправить worker, чтобы стабильно возвращал `result.image_url` при успешном рендере.
- [ ] Улучшить сообщение об ошибке из worker для UI.

## 3) Правки текущего UI-поведения
- [ ] Убрать отображение `Job ID` из UI.
- [ ] Убрать слово "асинхронно" из описаний.
- [ ] Добавить длинный пример промпта в `text_to_image` как информационную сноску (не как автозаполнение поля).

## 4) Image-to-text UX
- [ ] Сделать красивую drag-and-drop зону вместо обычного file input.
- [ ] Оставить fallback на клик/выбор файла.
- [ ] Добавить переключатель режима Narrator: `text` / `table`.
- [ ] Сохранить масштаб и качество изображения при загрузке (без ухудшения на frontend).

## 5) Редизайн (дерзкий, меньше градиентов)
- [ ] Перейти на акцент `#F36523`.
- [ ] Перестроить стартовый экран в split-layout на весь экран.
- [ ] Зафиксировать desktop split строго `50/50`.
- [ ] Добавить анимированное раскрытие выбранной половины.
- [ ] Упростить фон и снизить количество градиентов.

## 6) Проверка целостности
- [ ] Прогнать unit-тесты UI/API/lifecycle.
- [ ] Ручной smoke-тест обоих сценариев с живым backend+worker.
- [ ] Обновить документацию в `frontend/README.md` и `frontend/ui/README.md`.
